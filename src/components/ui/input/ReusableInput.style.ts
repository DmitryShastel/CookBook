import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginHorizontal: 12,
    marginVertical: 12,
    paddingHorizontal: 0,
    width: 'auto',
  },
  input: {
    color: '#000',
    fontSize: 16,
    padding: 10,
  },
  inputContainer: {
    borderColor: '#ccc',
    borderRadius: 8,
    borderWidth: 1,
    height: 40,
  },
});
