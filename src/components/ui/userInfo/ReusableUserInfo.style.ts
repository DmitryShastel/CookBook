import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  avatar: {
    marginRight: 10,
  },
  userContainer: {
    alignItems: 'center',
    flexDirection: 'row',
    marginBottom: 12,
  },
  userName: {
    color: '#333',
    fontSize: 16,
    fontWeight: '600',
  },
});
