import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  avatar: {
    borderRadius: 20,
    height: 40,
    marginRight: 10,
    width: 40,
  },
  userContainer: {
    alignItems: 'center',
    flexDirection: 'row',
    marginBottom: 12,
  },
  userName: {
    color: '#333',
    fontSize: 16,
    fontWeight: '600',
  },
});
